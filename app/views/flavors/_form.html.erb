<%= form_for @flavor do |f| %>
    <div class="form-group">
    <%= f.text_field :name, class: "form-control", placeholder:"Flavor" %><br>
    </div>
    <%= f.submit @flavor.persisted?,  "Update Flavor" : "Add Flavor", class: "btn btn-info"%>
<% end %>